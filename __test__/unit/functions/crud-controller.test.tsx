// Test controller logics
describe("Test controller logics", () => {
  describe("create", () => {
    it("be able to create user", () => {});
    it("can't create user if there are more than 10 users", () => {});
  });
  describe("read", () => {
    it("be able to create user", () => {});
    it("can't create user if there are more than 10 users", () => {});
  });
  describe("update", () => {
    it("be able to create user", () => {});
    it("can't create user if there are more than 10 users", () => {});
  });
  describe("delete", () => {
    it("be able to create user", () => {});
    it("can't create user if there are more than 10 users", () => {});
  });
});
